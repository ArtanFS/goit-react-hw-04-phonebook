import styled from 'styled-components';

export const FilterWrapper = styled('div')(() => {
  return {
    padding: '20px 0',
    display: 'flex',
    gap: '4px',
    flexDirection: 'column',
    width: '200px',
  };
});
